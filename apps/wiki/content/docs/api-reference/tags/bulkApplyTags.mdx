---
title: Bulk apply or remove tags
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Queue a background job to add or remove one or more tags across many
          contacts. Accepts either an explicit list of contact IDs (`mode:
          "ids"`), or a filter-based selection with optional exclusions (`mode:
          "query"`) — the same "select all matching" pattern used by the
          contacts bulk-action endpoints. Poll `GET /tags/bulk-apply/{jobId}`
          for progress.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Queue a background job to add or remove one or more tags across many contacts. Accepts either an explicit list of contact IDs (`mode: "ids"`), or a filter-based selection with optional exclusions (`mode: "query"`) — the same "select all matching" pattern used by the contacts bulk-action endpoints. Poll `GET /tags/bulk-apply/{jobId}` for progress.

<APIPage document={"./openapi.local.json"} operations={[{"path":"/tags/bulk-apply","method":"post"}]} />